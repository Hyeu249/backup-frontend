import styled from 'styled-components';
import WithDirection from '@iso/lib/helpers/rtl';

const CustomWrapper = styled.div`
  // background-color: #ebebeb;
  background-color: #ffffff;
  height: 100%;
`;

export default WithDirection(CustomWrapper);
